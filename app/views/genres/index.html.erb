

<h1>Genres</h1>

</br>

<%= link_to 'New Genre', new_genre_path, :class => "btn btn-custom-success" %>

</br></br>

<table class="table table-striped table-hover">
  <thead>
    <tr >
      <th>Name</th>

      <th colspan="2"></th>
    </tr>
  </thead>

  <tbody>
    <% @genres.each do |genre| %>
      <tr >
        <td data-link="<%= genre_path(genre) %>"><%= link_to genre.name, genre %></td>


        <td><%= link_to edit_genre_path(genre), :class=> "btn btn-primary" do %><span class="glyphicon glyphicon-pencil" aria-hidden="true"></span><%end%>
          <%# link_to  genre, method: :delete, data: { confirm: 'Are you sure?' }, :class =>'btn btn-danger' do %><!--<span class="glyphicon glyphicon-trash" aria-hidden="true"></span><%#end%>--></td>


         </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Genre', new_genre_path, :class => "btn btn-custom-success" %>
